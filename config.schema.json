{
  "pluginAlias": "3EMEnergyMeter",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "3EM Energy Meter Platform",
  "footerDisplay": "Requires Shelly EM device(s) on your local network.",
  "schema": {
    "type": "object",
    "required": ["name", "devices"],
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "3EMEnergyMeter",
        "description": "The name of the platform as it will appear in Homebridge UI."
      },
      "devices": {
        "title": "Devices",
        "type": "array",
        "description": "List of Shelly EM devices to monitor",
        "items": {
          "type": "object",
          "required": ["id", "name", "host"],
          "properties": {
            "id": {
              "title": "Device ID",
              "type": "string",
              "description": "Unique ID for the device."
            },
            "name": {
              "title": "Device Name",
              "type": "string",
              "description": "The name of the device as it appears in HomeKit."
            },
            "host": {
              "title": "Device IP",
              "type": "string",
              "description": "The local IP address of the Shelly EM device."
            }
          }
        }
      }
    }
  }
}