{
  "type": "object",
  "title": "3EM Energy Meter Platform",
  "description": "Configure the Shelly EM energy meters for Homebridge",
  "properties": {
    "name": {
      "title": "Platform Name",
      "type": "string",
      "default": "3EMEnergyMeter",
      "description": "The name of the platform shown in Homebridge"
    },
    "devices": {
      "title": "Devices",
      "type": "array",
      "description": "List of Shelly EM devices to monitor",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "title": "Device ID",
            "type": "string",
            "description": "Unique ID for the device"
          },
          "name": {
            "title": "Device Name",
            "type": "string",
            "description": "Name of the accessory shown in HomeKit"
          },
          "host": {
            "title": "Device Host",
            "type": "string",
            "description": "IP address of the Shelly EM device",
            "format": "ipv4"
          }
        },
        "required": ["id", "name", "host"]
      }
    }
  },
  "required": ["name", "devices"]
}