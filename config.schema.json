{
  "pluginAlias": "3EMEnergyMeter",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Shelly 3EM Energy Meter Plugin",
  "footerDisplay": "This accessory is only fully supported in the third-party HomeKit app Eve.",
  "schema": {
    "type": "object",
    "properties": {
      "devices": {
        "title": "Devices",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Device Name",
              "type": "string",
              "required": true,
              "description": "A friendly name for this Shelly 3EM device"
            },
            "host": {
              "title": "Device Host / IP",
              "type": "string",
              "required": true,
              "description": "The hostname or IP address of the Shelly 3EM (e.g. 192.168.1.50)"
            },
            "use_em": {
              "title": "Use EM Mode",
              "type": "boolean",
              "default": false,
              "description": "Enable if this device should be treated as an EM (single-phase) instead of a 3EM"
            }
          }
        }
      }
    }
  }
}